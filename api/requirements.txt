# Pinned for reproducible builds — bump deliberately, not incidentally.
fastapi==0.118.0
uvicorn[standard]==0.37.0
pydantic==2.13.5
psycopg[binary]==3.2.3
pglast==8.4
ollama==0.6.2  # local generation backend — see api/app/llm/client.py for why (no Anthropic key available)
pyyaml==6.0.3
pyjwt==2.10.1
email-validator==2.2.0
pytest==9.1.1
pytest-asyncio==0.25.2
